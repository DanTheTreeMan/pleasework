<div class="row nav-bar">
  <nav class="top-bar column" data-topbar>

    <!-- Title and Menu options section -->
    <ul class="title-area">
      <li class="name">
        <h1>
          <a href="/">
            <i class="fi-trees"></i> GeomeTree
          </a>
        </h1>
      </li>
      <li class="toggle-topbar menu-group"><a href="#"><span>Menu</span></a></li>
      <li class="menu-group"><%= link_to "About Us", about_path %></li>
      <%if signed_in?%>
        <li class="menu-group"><%=link_to current_user.try(:email), edit_user_registration_path%></li>
        <li class="menu-group"><%=link_to "Logout", destroy_user_session_path, method: :delete%></li>
      <%else%>
        <li class="menu-group"><%=link_to "Register", new_user_registration_path%></li>
        <li class="menu-group"><%=link_to "Login", new_user_session_path%></li>
      <%end%>
      <li class="menu-group"> <%= render 'layouts/cart_text' %> </li>
    </ul>

    <!-- Left Nav Section -->
    <section class="top-bar-section search-bar">
      <ul class="left">
        <li class="has-form">
          <div class="row collapse">
<!-- other search bar went here -->
            <div class="large-4 small-3 columns">
              <%= render 'layouts/search_bar' %>
            </div>
          </div>
        </li>
      </ul>
    </section>
  </nav>
</div>
